
const state = {
    currentUser: null
};

const getters = {
    currentUser: state => state.currentUser
}

const actions = {}

const mutations = {}

export default {
    state,
    getters,
    actions,
    mutations
  };